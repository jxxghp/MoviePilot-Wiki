---
title: 发行说明
description: 需要关注的重要版本变更
published: 1
date: 2026-08-07T23:12:14.853Z
tags: 
editor: markdown
dateCreated: 2024-05-30T09:49:17.992Z
---

> 访问 [此处](https://github.com/jxxghp/MoviePilot/releases) 了解所有的版本更新日志，此页面只针对历史上的重大变更做一些说明，在进行升级前请了解这些信息并做好准备。
{.is-success}

# v3.0.0

MoviePilot V3 为独立主版本，站点索引和认证资源使用 V3 资源体系，不回退到 V2 资源，也不以内置兼容 V2 配置和数据库为目标。切换前请备份原实例，并按 [V3版本说明](/v3) 准备独立的 V3 配置和资源目录。

V3 的主要变化：

- 电影、电视剧流程继续保留，媒体类型新增“音乐”。
- 音乐支持元数据搜索、站点资源搜索、下载、订阅和音频文件整理。
- 推荐和探索增加音乐入口。
- 综合站点可以声明音乐分支，不要求站点本身是音乐专站。
- 站点资源目录切换为 `resources.v3`，站点包切换为 `user.sites.v3.bin`。

音乐不包含内置媒体库、歌单、歌手库或播放器，播放和媒体库管理仍由 Navidrome、Emby、Jellyfin、Plex 等软件负责。

{.is-info}

# v2.12.0
更换浏览器内核为 Cloak Browser (https://cloakbrowser.dev/)，以提升穿透 Cloudflare 人机验证能力

- 需要重新拉取新镜像才能生效
- 首次启动会下载浏览器内核（大约140M），需要调整docker容器浏览器内核缓存目录映射：
> /moviepilot/.cache/ms-playwright ==> /moviepilot/.cloakbrowser


# v2.5.9
- 此版本及以后将不再支持Python 3.11环境，建议重新拉取最新镜像，否则后续将无法自动更新站点资源

# v2.0.1
- MoviePilot V1版本停止功能更新，仅修复重大Bug以及同步更新站点资源包。V2版本新增多项新特性，部分功能重新设计开发，使用独立境像，无法通过V1版本自动升级，同时也不兼容旧版本配置和数据库文件。

# v1.9.8
- 支持直接整理阿里云盘和115网盘内文件，**此版本后仅做问题修复，不再新增功能或对现有功能进行大幅改动。**

# v1.9.4
- 优化了硬链接的处理逻辑，兼容极空间的同时**跨盘不再会自动变成复制**了，**如果你原来的目录映射不符合硬链接的条件，升级到此版本后会直接报错`-1`。**


# v1.9.1
- 目录设置全新改版，按优先级匹配，支持多目录，存量目录配置会自动升级为新格式，**但对于动漫原来是单独一级目录的需要手工调整。**
- **注意：** 因媒体库目录设置结构调整，`目录监控`插件指定目的路径时不再支持自动创建下级分类目录。建议插件中不指定目的目录，而是在`设定->目录`中设定好目录结构让系统自动匹配使用（根据需要打开同盘优先选项）。实际上如果是可以通过分类来指定目录的，建议直接使用内建的下载器监控功能，目录监控仅是个插件，仅适用于不通过MoviePilot自动下载的文件监控整理。

**设置项变化对比：**

![dir.png](/dir.png)



