---
title: V3 版本说明
description: MoviePilot V3 的版本边界、音乐支持和资源体系
published: 1
date: 2026-08-07T13:30:00.000Z
tags: V3, 音乐
editor: markdown
dateCreated: 2026-08-07T13:30:00.000Z
---

# MoviePilot V3

MoviePilot V3 是独立的主版本，重点是继续复用电影、电视剧的自动化流程，同时把音乐作为并列媒体类型接入搜索、下载、订阅和整理。

V3 不以内置音乐媒体库为目标。音乐的播放、歌单、歌手库和文件索引继续交由 Navidrome、Emby、Jellyfin、Plex 等现成媒体软件负责；MoviePilot 负责把音乐资源找到、下载并整理到指定目录。

{.is-success}

## 版本边界

- V3 使用独立的站点索引和资源版本，不回退到 V2 资源，也不以 V2 配置和数据兼容为目标。
- 升级或切换前请备份原有实例，并为 V3 准备独立的配置目录、数据库和资源目录；不要直接覆盖仍在运行的 V2 实例。
- V3 站点索引资源使用 `resources.v3`，站点包文件名为 `user.sites.v3.bin`，索引版本为 `3.0.0`。
- 站点 Cookie、User-Agent、代理和限流配置仍由 MoviePilot 管理，站点索引只负责公开的解析规则和分类定义。

## 编译与镜像发布

- V3 后端、前端和站点资源分别从各仓库的 `v3` 分支构建，版本标签统一使用 `v3.x`。
- V3 Docker Hub 镜像为 `jxxghp/moviepilot-v3`，正式版发布版本号标签（例如 `3.0.0`）和 `latest`；Beta 使用 `beta` 标签。
- V3 GitHub Container Registry 镜像为 `ghcr.io/jxxghp/moviepilot-v3`，不再向无后缀的 `moviepilot` 镜像写入 V3 构建结果。
- Docker 构建只读取 `MoviePilot-Resources` 的 `v3/resources.v3/`，运行时自动更新也只筛选 `v3.*` 后端和前端发布版本。
- `MoviePilot-Build` 生成的站点索引和编译扩展通过面向 `MoviePilot-Resources/v3` 的 PR 发布，避免覆盖 V2 资源。

## V3 支持的媒体类型

| 媒体类型 | 主要能力 |
| --- | --- |
| 电影 | 元数据搜索、站点资源搜索、下载、订阅、整理 |
| 电视剧 | 元数据搜索、站点资源搜索、下载、订阅、追更和整理 |
| 音乐 | 歌曲/专辑/艺术家搜索、音乐资源搜索、下载、订阅和音频文件整理 |

音乐会沿用现有的搜索、下载、订阅、历史和通知链路，不另起一套下载器或任务队列。

## 音乐使用流程

```text
搜索歌曲、专辑或艺术家
        ↓
选择音乐元数据
        ↓
搜索站点音乐资源
        ↓
选择资源并提交下载，或创建音乐订阅
        ↓
下载完成后识别音频标签和技术参数
        ↓
按音乐重命名格式整理到目标目录
        ↓
由现有媒体服务器负责扫描和播放
```

### 音乐元数据

V3 当前通过 Python 模块完成音乐专用识别：

- `MusicMeta`：统一歌曲、专辑、艺术家、曲序、碟号、年份、ISRC、音频格式等解析结果。
- `MusicBrainzModule`：搜索录音、读取详情、补全发行和艺术家信息。
- `AudioMetadataHelper`：读取本地音频标签和采样率、位深、码率、时长等技术参数。
- `ListenBrainzModule`：提供推荐和探索榜单，不承担本地音乐库管理。

目前 Rust 扩展并没有音乐专用识别实现。Rust 已支持通用 `MetaInfo` 解析、站点索引解析、资源过滤以及音乐分类映射；音乐字段识别和音频标签读取仍由 Python 侧完成。后续如需 Rust 化，应以兼容 `MusicMeta` 字段和 Python 回退路径为前提单独设计。

### 站点资源

音乐站点分为两类：

1. **音乐专用站点**：站点本身主要提供音乐资源。
2. **综合站点的音乐分支**：站点主体可能是影视站，但站内存在无损音乐、唱片、演唱会或音乐视频分类。

搜索音乐资源时，MoviePilot 会根据 V3 索引中的 `music` 分类选择对应分支；没有音乐专用搜索路径时，会复用站点的通用搜索路径。站点是否为“音乐站点”不再作为唯一判断条件。

站点 Cookie 配置方式与影视站点一致，详见 [站点](/site)。

### 音乐整理

音乐整理使用 `设定 -> 目录` 中独立的音乐重命名格式，默认结构为：

```text
艺术家/专辑 (年份)/曲序 - 歌曲.扩展名
```

格式可使用 `artist`、`album_artist`、`album`、`title`、`track`、`disc`、`year`、`fileExt` 等音乐字段。MoviePilot 只负责下载后识别和整理，不提供音乐播放、歌单同步或内置音乐库。

## 搜索、推荐和探索入口

- 聚合搜索中新增“音乐”媒体类型，可按歌曲、专辑或艺术家查询。
- 音乐详情可继续进入站点资源搜索或创建订阅。
- 推荐和探索增加音乐分类，并提供 ListenBrainz 榜单入口。
- 音乐订阅在订阅页面中独立显示，但底层仍复用现有订阅刷新和下载逻辑。

接口使用者可参考以下 V3 API：

| 接口 | 用途 |
| --- | --- |
| `GET /api/v1/music/search` | 搜索音乐元数据 |
| `POST /api/v1/music/recognize` | 根据来源和媒体 ID 获取音乐详情 |
| `GET /api/v1/music/explore` | 浏览月度热门音乐 |
| `GET /api/v1/recommend/music_weekly` | 浏览本周推荐音乐 |

## 当前不提供的功能

- 音乐媒体库、歌曲/专辑文件索引和在线播放。
- 歌单导入、歌单管理和跨媒体服务器同步。
- 歌手库、艺术家合并和本地收听历史。
- 自动向源音频文件写入封面、歌词或标签（当前主要读取标签用于识别和整理）。
- 独立的音乐下载器、音乐站点登录体系或音乐任务队列。

## 升级和排障建议

1. 升级前备份 V2 配置目录和数据库。
2. 使用 V3 对应的镜像或源码分支，并确认资源目录为 `resources.v3`。
3. 在 `设定 -> 关于` 检查站点资源版本，确认已加载 `user.sites.v3.bin`。
4. 先用一个已登录站点搜索 `FLAC` 或具体艺术家，确认搜索结果分类为“音乐”。
5. 再创建单条音乐订阅，确认下载器、下载目录和音乐重命名格式均正确后扩大使用范围。

如果站点可以访问但结果被归类为“未知”，优先检查站点资源是否为 V3、站点 Cookie 是否有效，以及该站点的音乐分类是否仍在站点页面中存在。
